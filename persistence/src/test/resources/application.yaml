spring:
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml
  jpa:
    hibernate:
      ddl-auto: validate
      
logging:
  level:
    root: DEBUG
    '[com.duluthtechnologies.ocpi]': DEBUG